# Optional app URL used for absolute links when needed.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# The current portfolio demo uses in-memory SQLite mock data and does not
# require Supabase credentials.
